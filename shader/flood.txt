// https://github.com/lachlanmcdonald/magicavoxel-shaders
// v. 0.2.0
// xs flood [n]

float map( vec3 v ) {
    float index = voxel( v );
    if ( index == 0.0 ) {
        if ( v.z < iArgs[0] ) {
            return iColorIndex;
        }
    }
    return index;
}
