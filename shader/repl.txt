// https://github.com/lachlanmcdonald/magicavoxel-shaders
// v. 0.2.1
// xs repl [index]

float map( vec3 v ) {
    float index = voxel( v );
    if ( index == iColorIndex ) {
        return float(iArgs[0]);
    }
    return index;
}
